[project]
name = "budget-planner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Peyton Geiger", email = "pag0038@auburn.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.11.2",
]

[project.scripts]
m2-budget-planner = "budget_planner:main"
single-panel = "budget_planner.budget_category_panel:main"
single-card = "budget_planner.summary_card:main"
budget-planner = "budget_planner.main:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
