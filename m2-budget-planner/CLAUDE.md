# Assignment boundaries

All code and documentation on `m2/human/budget-ui` are written by me, the student, without generative-AI assistance. This branch is now the frozen student-only baseline.

You may inspect the project. On `m2/ai/budget-finalization`, you may create or modify `style.qss` and `README.md`. At my explicit request, you may modify `src/budget_planner/main.py` solely to load `style.qss`. Other Python-source changes are allowed only when I explicitly ask you to diagnose and make a targeted fix to required application behavior. Before making a fix, explain the planned change and stop after completing that fix. Make only the change I explicitly request. Do not add other functionality.

Do not modify `pyproject.toml`, a lock file, or `CLAUDE.md`. Do not modify files outside the `m2-budget-planner` project directory.

If a requested change would require modifying a file other than those permitted above, explain why and stop. Do not make that change.

You may use Git commands only while `m2/ai/budget-finalization` is checked out. You may commit and push work on that branch. Do not switch to, commit to, merge into, rebase, reset, force-push, or delete `m2/human/budget-ui` or `main`. Do not move AI-assisted changes back to `m2/human/budget-ui`.
